package basics.ch_15_enum;

/*
 * An Enum is a special class which contains
 * list of constants
 * 
 * To create a enum, use the "enum" keyword instead of 
 * "class" keyword, and separate the constants with a comma.
 * 
 * constants should be in uppercase letters. 
 */

public class EnumTheory {

}
