Protected Keyword
~~~~~~~~~~~~~~~~~~

The protected access modifier is specified using the keyword protected

The methods or data members declared as protected 
are accessible within the same package or 
for sub classes in different packages.